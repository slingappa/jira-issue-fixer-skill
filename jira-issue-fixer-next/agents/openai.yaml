interface:
  display_name: "Jira Issue Fixer Next"
  short_description: "Reproduce, debug, fix, validate, and commit Jira issues"
  default_prompt: "Use $jira-issue-fixer-next to reproduce a Jira issue, isolate root cause, implement a minimal fix, validate with scripted repro, and prepare a clean commit. First use jira-api skill for full Jira intake; if jira-api is unavailable, stop and ask user to install it."
